<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>Acme.Shop.Data.Models</AssemblyName>
    <RootNamespace>Acme.Shop.Data</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Contracts only. This project deliberately does not reference Entity Framework, so a domain can
         depend on the repository interfaces without depending on how they are implemented. -->
    <PackageReference Include="MagicCSharp.Data" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Shop.Domains\Orders\Models\Acme.Shop.Domains.Orders.Models.csproj" />
  </ItemGroup>

</Project>
